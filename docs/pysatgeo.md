
# pysatgeo package

The top-level `pysatgeo` package currently exposes package metadata only:

- `__author__`
- `__email__`
- `__version__`

Use the module pages for the current public functions:

- `pysatgeo.raster`
- `pysatgeo.vector`
